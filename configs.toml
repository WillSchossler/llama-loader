# Configurations file. 


# The root folder where the models are stored. Th search is recursive. 
# Watch out for correct separator. Use the standard "/".
root =  "pytests/example"


# The command to open Your file editor.
# I recommend VS Code (don't forget the sufix ".cmd" if you're using windows).
editor = "code.cmd"


# The path to your browser's executable file.
# (OPTIONAL: Delete if you don't want to automatically open the browser)
browser_path = "C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"
